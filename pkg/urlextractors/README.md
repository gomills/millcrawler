# /urls_extractors

Here reside functions to extract url from files according to their extension.

Currently robots.txt, .html and .js get parsed and worked exhaustively. In case others were to be scanned for urls, they fall to regex, such as sitemap.xml