# /urls_extractors

Here reside functions to extract url from files according to their extension.

robots.txt, .html and .js get parsed and worked exhaustively. 

sitemap.xml gets regexed.

others simply are ignored in the crawler.