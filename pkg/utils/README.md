# /utils

Here reside utility functions that are generic enough not to be in a dedicated package.

We have initial_urls.go: used to generate the initial seed of brute-forced urls to crawl.

We have is_local_url.go: utility to learn if a url is local to a domain.