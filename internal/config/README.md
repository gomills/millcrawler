# /config

In this file there's the hardcoded config for url and crawling heuristics. It used to load a config.json but for simplicity, both of builds and of code, it was deprecated and now it's hardcoded in .go file.